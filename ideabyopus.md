# ideabyopus — Sa_aohue 追加機能アイデア

`docs/KNOWN_ISSUES.md` の修正が済んだ状態を前提に、その土台があって初めて成立する(あるいは劇的に安くなる)アイデアだけを挙げる。

**前提としている修正**

| | 前提 | 出典 |
|---|---|---|
| P1 | 検出器が実ガウシアンのXDoGになり、σが可変パラメーターになっている | KNOWN_ISSUES 1-1, 1-2 |
| P2 | しきい値がカーネルの正ローブL1で正規化され、σや検出器を変えても意味が保たれる | KNOWN_ISSUES 1-4 |
| P3 | 可分ガウシアン化で近傍コストが O(r²) から O(r) になっている | KNOWN_ISSUES 1-6 |
| P4 | ブラーの半径量子化が解消され、σが連続に指定できる | KNOWN_ISSUES 2-1 |
| P5 | `render()` の2つの画素ループが並列化され、フレーム時間の予算が空いている | KNOWN_ISSUES 3-1, 3-2 |
| P6 | 色調整の定義が `src/core.h` に一本化され、AE版とYMM版で式が共通 | FEATURE_PROPOSALS 提案C |

`docs/FEATURE_PROPOSALS.md` に既にある案(輝度・色相・自然な鮮やかさの移植、Mask to Alpha、色相セレクター、マスク品質切替など)はここでは繰り返さない。

各案は **何ができるか / なぜ今か / 実装の当て / コストとリスク** で書く。★は推奨度。

---

## A. 検出器を活かす

### A-1. マルチスケール線検出 `Line Detail` ★★

**何**: σを1つではなく2〜3段(例 σ, 2σ, 4σ)で検出し、重み付きで合成する。細い描き込みと太い輪郭を同時に拾える。

**なぜ今か**: P1でσが可変になり、P3で1段あたりのコストが O(r) に落ちる。現状の O(r²) 円盤では多段化は現実的でなかった。

**実装**: `lineArt` を「1スケール分の応答を返す関数」に切り出し、スケール数ぶん呼んで `max` または重み和で合成する。P2の正規化があるので、スケール間で応答の大きさが揃い、単純な合成が意味を持つ。UIは `Line Detail`(1〜3段)と `Detail Balance`(細い側と太い側の比)の2つで足りる。

**コスト**: 段数に比例。σが倍々なので実コストは1段目の約2倍(可分なので半径に線形)。

**リスク**: 低い。段数1で従来と同一。

---

### A-2. 解像度非依存モード `Scale Mode: Pixels / Relative` ★★★

**何**: σと`Radius`を「画素」ではなく「フレーム短辺に対する比」で指定できるようにする。1080pで詰めた設定が4Kでもプロキシでもそのまま通る。

**なぜ今か**: KNOWN_ISSUES 1-2 で、検出器の応答が解像度に強く依存することを測定した。P1でσが露出して初めて「解像度に合わせてσを動かす」という操作が可能になる。加えて KNOWN_ISSUES 2-6 の `downsample` 追従はこの機能の一部として自然に実装できる。

**実装**: `Scale Mode` ポップアップ(既定 Pixels = 従来)。Relative のとき、σと`Radius`に `min(width,height)/1080` を掛ける。AEの低画質表示は `in_data->downsample_x/y` を同じ係数に畳み込む。YMM4は `EffectDescription` の解像度から同様に求める。

**コスト**: ほぼゼロ(パラメーターのスケーリングのみ)。

**リスク**: 低い。既定Pixelsで従来通り。基準解像度(1080p)を何にするかは決めの問題なので、UIに明記する。

---

### A-3. 自動しきい値 `Auto Threshold` ★★★

**何**: しきい値を固定値ではなく「フレームの `D` 分布の上位N%」で決める。素材が変わっても、線として拾う量が一定になる。

**なぜ今か**: KNOWN_ISSUES 1-2 の測定で、適正しきい値が素材のノイズ量に強く依存することが分かっている。今は `Line Threshold` を素材ごとに手で取り直すしかない。P2の正規化と組み合わせると、「上位2%を線とする」という指定がσ・解像度・検出器をまたいで意味を持つ。

**実装**: `D` を求めた直後にヒストグラムを1パスで作り、目標パーセンタイルに対応する値をしきい値にする。全画面を持っているので追加の読み出しは不要。UIは `Threshold Mode: Manual / Auto` と `Line Coverage`(0.1〜20%)。

**時間的安定性**: フレームごとにパーセンタイルが動くとチラつく。対策として、**ヒストグラムは1/4解像度で十分**(スカラーを1つ決めるだけなので)。前後フレームからスカラーを取りたい場合も、低解像度フレームのチェックアウトで済み、D-1のようなマスク全体の多フレーム保持は要らない。

**コスト**: 1/4解像度のヒストグラム1パス。無視できる。

**リスク**: 中。線が本当に無いフレーム(真っ白な背景)でも「上位2%」を拾ってしまうので、`D` の絶対値の下限を併用する必要がある。

---

### A-4. 流れに沿ったDoG (FDoG) ★★

**何**: エッジ接線流(ETF)を求め、線の方向に沿って `D` を積分してから、直交方向にしきい値を掛ける。途切れた線が繋がり、方向性のないノイズが消える。Kang らの Flow-based DoG。

**なぜ今か**: KNOWN_ISSUES 1-3 で、現行検出器の応答が線の向きで57%変わることを測った。等方ガウシアン(P1)で17%まで下がるが、FDoGはこれを「方向依存を消す」のではなく「方向を積極的に使う」方向の解であり、線画的な絵作り(このプラグインの目的)に最も効く。

**実装**: 構造テンソルから主方向を求め、数回の平滑化でETFを作る(2〜3パス)。その流れに沿って1次元のDoGを積分する。`lineArt` の内部を差し替える形で、`Detector` ポップアップに3つ目の選択肢として足す。

**コスト**: 中。構造テンソル+ETF平滑化+流れに沿った積分で、等方XDoGの3〜5倍程度を見込む。P5で空いた予算内に収まるかは実測が要る。

**リスク**: 中〜高。パラメーターが増える(ETFの平滑化回数、積分長)。GPU実装(YMM版)では流れに沿った可変長積分がシェーダーで書きにくい。**AE版のみ先行し、YMM版は等方XDoGのままにする**割り切りも選択肢。

---

### A-5. 診断ビュー `Diagnostic View` ★★

**何**: `Output Mode` に診断用の表示を足す。(a) `D` の生値をヒートマップ、(b) しきい値をまたぐ寸前の画素を色で表示、(c) 検出された線の被覆率を数値で出す。

**なぜ今か**: KNOWN_ISSUES 1-2 の誤検出60%は、**ユーザーが見て気づけない**のが問題の本質だった。Composite出力では「なんとなく効きすぎている」としか分からない。しきい値近傍の画素を可視化すれば、チラつく画素が一目で分かる。

**実装**: 出力段だけの変更。マスク計算には触れない。(c) はAEなら `PF_ADD_TOPIC` 内の読み取り専用テキスト、YMM4なら別途表示手段が要るので (a)(b) を優先。

**コスト**: ほぼゼロ。

**リスク**: なし。A-3(自動しきい値)の調整UIとしても効く。

---

## B. 色を活かす

### B-1. 作業色空間の選択と自動取得 ★★★

**何**: `RGB Encoding` を「転送関数(sRGB/Linear)」だけでなく「原色(sRGB / Display P3 / Rec.2020)」まで指定できるようにする。AE側は可能なら作業色空間を問い合わせて自動設定する。

**なぜ今か**: これは機能追加であると同時に、現状の正しさの穴でもある。`src/core.h` の `toLab` が使っている行列(`0.4122214708` 以下)は **linear sRGB → LMS** の行列であり、OKLabの定義がそうなっている。作業色空間がDisplay P3やRec.2020のプロジェクトに適用すると、同じRGB値を別の色として解釈するため、**色相と彩度がずれた状態で増幅する**。`Linear sRGB` という選択肢名は正直だが、広色域プロジェクトでは正しい選択肢が存在しない。

**実装**: 原色ごとに「原色→XYZ→LMS」を合成した3×3行列を用意し、`toLab`/`fromLab` を行列パラメーター付きにする。`gamut()` は今も作業色空間の[0,1]立方体を見ているだけなので変更不要。AE側はAEGPのカラー設定スイートで作業色空間を取得できるか要確認(取れなければ手動指定のまま、選択肢だけ増やす)。

**コスト**: 低い。行列が定数から変数になるだけ。

**リスク**: 低い。既定sRGBで従来通り。P6で式が一本化されていれば、YMM版へは同じ行列を渡すだけで済む。

---

### B-2. 色域マッピングの改善 ★★

**何**: 色域外になったとき、現在は a/b を等倍で縮めている(彩度だけを落とす)。OKLabのCUSP法のように **Lも少し動かしながら** 色域境界へ寄せると、同じ色域内でより高い彩度に到達できる。

**なぜ今か**: KNOWN_ISSUES 3-3 で二分探索を「目標を先に試す」形に直すと、色域外画素だけが探索に入るようになる。そこが分離されて初めて、探索の中身を差し替える意味が出る。

**実装**: `chroma()` の二分探索を、a/b等倍の直線ではなく、L方向に少し傾けた直線上の探索に置き換える。UIは `Gamut Mapping: Clip Chroma / Preserve Hue (CUSP)` の2択で足りる。

**コスト**: 探索回数は同じ。1回あたりの計算がわずかに増える。

**リスク**: 中。Lを動かすので「Lは維持する」という現行の設計方針(README 1章)からの逸脱になる。既定は従来のままにして、選択肢として足す。

---

### B-3. 彩度の指定単位 `Amount Mode: Relative / Absolute` ★

**何**: 現在の `Amount` はa/bの**倍率**なので、元々彩度の高い画素ほど絶対的な増加量が大きい。OKLChのCに**絶対量**を足すモードを足す。

**なぜ今か**: P6で `Vibrance`(高彩度部の抑制)がAE版にも入る。VibranceはこのモードのソフトなバージョンなのでUI上の整理が必要になり、そのタイミングで両方を明示的に選べるようにするのが素直。

**コスト**: ほぼゼロ。**リスク**: 低い。ただしパラメーターの意味が増えるので、UIの説明を丁寧に書く必要がある。

---

## C. マスクを外へ出す

### C-1. 線マスクの他用途への転用 ★(scope注意)

線に沿った **グロー**、**局所コントラスト強調**、**色収差**、**線自体の着色**。どれも「良いマスクがあるなら安く作れる」ものではある。

ただし **このプラグインの名前と目的は彩度(aohue)であり、これらは別エフェクトにすべき** というのが率直な判断。`Mask to Alpha`(FEATURE_PROPOSALS 提案F)でマスクを外に出せるようにすれば、AEの既存エフェクトやYMM4の他エフェクトと組み合わせて同じことができる。**C-1は「実装しない」方向を推す。** 実装するとしても、共通の `core.h` を使う別プラグイン(`Sa_aoline` など)として切る。

---

## D. 時間方向

### D-1. 時間中央値によるチラつき除去 ★

**何**: 前後1フレームのマスクと合わせて3フレームの中央値を取る。単発のチラつき画素が消える。

**なぜ今か**: FEATURE_PROPOSALS 提案G では「AEでは多フレームチェックアウトのメモリが厳しい」として保留した。前提が2つ変わる — P1でノイズ由来のチラつき自体が桁で減り(誤検出60%→0.3%)、A-3で低解像度のスカラー取得という軽い代替が見えた。したがって **D-1は「まだやらない」判断のままでよい**。まずA-3を入れて残るチラつきを測り、それでも足りなければ着手する、という順序を明示しておく。

**コスト**: 高い(全画面マスク×3)。**リスク**: 動く被写体で残像。

---

## E. 実行基盤

### E-1. タイル化 (halo付きROI) ★★

**何**: 全画面チェックアウト(`req.rect` を ±1000000)をやめ、必要な範囲だけを取る。

**なぜ今か**: P1で検出カーネルの広がりが `3σ` と有限に確定し、P4でブラーのσが連続かつ既知になる。halo は `ceil(3σ) + Radius` と厳密に計算できる。現行の円盤+box量子化では、この見積もりが曖昧だった。

**効果の実測見積もり** (overdraw = タイル面積比):

| タイル | σ | Radius | halo | overdraw |
|---:|---:|---:|---:|---:|
| 256 | 1.0 | 6 | 9 | 1.15x |
| 256 | 1.6 | 24 | 29 | 1.50x |
| 512 | 1.6 | 24 | 29 | 1.24x |
| 512 | 1.6 | 128 | 133 | 2.31x |
| 512 | 1.6 | 512 | 517 | **9.12x** |

**判断**: `Radius` が小さいうちは有効だが、`Radius` が大きいとhaloが支配して逆効果になる。したがって **「Radiusがタイル寸法に対して十分小さいときだけタイル化し、それ以外は全画面に落とす」ハイブリッド**にすべき。無条件のタイル化は性能を悪化させる。

**コスト**: 実装は中程度(preRenderのROI計算とテスト)。**リスク**: 中。KNOWN_ISSUES 2-5(境界処理の不統一)を先に直さないと、タイル境界に継ぎ目が出る。

---

### E-2. AE版のGPU実行 ★★

**何**: AEのSmartFX GPU経路(`PF_Cmd_GPU_DEVICE_SETUP` ほか)に対応する。

**なぜ今か**: 修正後のパイプラインは「可分ガウシアン + per-pixel演算」だけになり、GPUに素直に載る形になる(現行の非可分円盤・prefix sumはそうではなかった)。しかも **YMM版が既にHLSLで同じ式を実装している**。P6で式が一本化されていれば、シェーダーを両者で共有できる。

**実装**: `core.h` を「式の定義」と「CPUループ」に分け、式側をHLSL/GLSLへ機械的に写せる形に保つ。まずAE実機でCPU版を計測し(README 6章の通り未検証)、必要性を確認してから着手する。

**コスト**: 高い。**リスク**: 高い(SDKのGPU経路は8bpc非対応など制約が多い)。P5のCPU並列化で足りるなら不要かもしれない、という判断を先に行う。

---

## F. 使い勝手

### F-1. プリセット ★★

**何**: 「アニメ塗り」「実写」「線画抽出」「弱め」の4つ程度。

**なぜ今か**: KNOWN_ISSUES 1-2 の測定が示すのは、**素材によって適正パラメーターが大きく違う**ということである。σ・しきい値・`Radius`・`Amount` が絡むので、初見のユーザーが当てるのは難しい。A-3(自動しきい値)と組み合わせると、プリセットが素材差を吸収できるようになり、初めて実用的なプリセットが作れる。

**実装**: AEは `.ffx` を配布物に同梱。YMM4はプリセット機能に載せるか、READMEに数値表を書く。コード変更は不要かごく僅か。

**コスト**: ほぼゼロ。**リスク**: なし。A-2(解像度非依存)とA-3が入っていないとプリセットが素材ごとに破綻するので、**A-2/A-3の後に行う**。

---

## 優先度

| 順 | 案 | 理由 | 前提 |
|---|---|---|---|
| 1 | **A-2 解像度非依存** | 実害(KNOWN_ISSUES 2-6)の解消を含み、コストがほぼゼロ | P1 |
| 2 | **A-5 診断ビュー** | 出力段のみ。以降の調整すべての土台になる | なし |
| 3 | **B-1 作業色空間** | 広色域プロジェクトでの正しさの穴を塞ぐ | P6 |
| 4 | **A-3 自動しきい値** | 素材差という最大の運用コストを吸収する | P1, P2 |
| 5 | **F-1 プリセット** | A-2/A-3 が入って初めて成立する | A-2, A-3 |
| 6 | **A-1 マルチスケール** | σ可変の自然な発展。安い | P1, P3 |
| 7 | **E-1 タイル化(条件付き)** | Radiusが小さいときのみ有効。無条件は逆効果 | P1, P4, 2-5 |
| 8 | **B-2 色域マッピング** | 表現の上積み | 3-3の修正 |
| 9 | **A-4 FDoG** | 品質は最も伸びるがコストとリスクが大きい | P1, P3, P5 |
| 10 | **E-2 GPU** | CPU並列化で足りるか判断してから | P5, P6 |

**採らない**: C-1(別プラグインに切るべき)、D-1(P1とA-3で問題が消える可能性が高いので、測ってから)。

## この文書の前提の弱点

- 上記の判断は `docs/KNOWN_ISSUES.md` の修正がすべて入った状態を仮定している。実際には P1(XDoG化)が最も重く、これが入らない限り A-1/A-2/A-3/E-1 の前提が揃わない。
- AE実機での描画は依然として未検証(README 6章)。E-1・E-2 の必要性は実機の計測結果次第で判断が変わる。
- コストの見積もりは4論理CPUのLinuxコンテナでの測定に基づく相対値であり、実際のWindows/AE環境での比率とは異なる。
